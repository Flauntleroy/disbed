// Konfigurasi kamar yang di-exclude (tidak ditampilkan)
// Tambahkan kode kamar yang ingin di-exclude ke array ini

const excludedRooms = [
    // Contoh:
    // 'AGR1.01',
    // 'AGR2.02',
];

// Konfigurasi bangsal yang di-exclude (tidak ditampilkan)
// Tambahkan kode bangsal yang ingin di-exclude ke array ini

const excludedWards = [
    // Contoh:
    // 'IGD',
    // 'RJ',
];

export { excludedRooms, excludedWards };
